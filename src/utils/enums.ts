export enum Roles {
  ADMIN = 'admin',
  USER = 'user',
}

export enum TransferModulePermissions {
  view_transfers = 'view_transfers',
  edit_transfers = 'edit_transfers',
  delete_transfer = 'delete_transfers',
}
